## welcome to markdown file brewer

the following files written by markdown format,and those will be show up on website by html format.
* math format:
$$ y(x,t) = y = A \sin(kx - \omega t) $$
* code format:
```c
#include <stdio.h>
```